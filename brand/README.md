# brand/

Logo and icon assets embedded in the `parsec` binary (tray icon) and the
native installers. Referenced by path from `packages/proxy/src/tray` and
`packages/installer`.

The MIT license covers the code in this repository, not the parsec name or
logo. See the trademark note in `THIRD_PARTY.md` before reusing these files
in a fork.
