# Keep the build context lean. NOTE: bundle/ is intentionally NOT ignored —
# an image build may drop the checkpoint there to bake it into the image.
__pycache__/
*.pyc
.pytest_cache/
tests/
.ruff_cache/
*.egg-info/
.venv/
